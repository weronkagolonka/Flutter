import 'package:does_it_fit/screens/DisplayCalc.dart';
import 'package:flutter/material.dart';
import './Dropdown.dart';
import '../models/Unit.dart';
import '../models/Dependencies.dart';
import './home.dart';

/*
double _value = drop1.getCurrentUnit().getMin().roundToDouble();
int roundedLabel = _value.round();

class MySlider extends StatefulWidget {

  _MySlider createState() => _MySlider();

  double getSliderValue() {
    return _value;
  }
}

class _MySlider extends State<MySlider> {

  Widget build(BuildContext context) {
    return SliderTheme(
      data: SliderTheme.of(context).copyWith(
        activeTrackColor: Colors.red[700],
        inactiveTrackColor: Colors.red[100],
        trackShape: RectangularSliderTrackShape(), //custom track: | | | | | |
        trackHeight: 5.0,
        thumbColor: Colors.redAccent,
        thumbShape: RoundSliderThumbShape(enabledThumbRadius: 12.0), //custom thumb: doesn't move
        overlayColor: Colors.blue.withAlpha(32),
        overlayShape: RoundSliderOverlayShape(overlayRadius: 28.0), 
      ), 
      child: Slider(
        min: min,
        max: max,
        value: _value, 
        divisions: max.round(),
        onChanged: (double newValue) {
          setState(() {
            _value = newValue;
            toDisplay = display();
          });
        }
      ),
    );
  }
}
*/
 